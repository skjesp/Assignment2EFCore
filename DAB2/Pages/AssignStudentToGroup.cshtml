@page
@model AssignStudenToGroupModel
@{
    ViewData["Title"] = "Assign student to group";
}

<h3>@ViewData["Title"]</h3>


<div class="row">
    <div class="col-md-6">
        <form method="post">
            <h5>Select a student and a group and assign.</h5>
            <hr/>
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="StudentGroup.StudentId"></label>
                <select asp-for="StudentGroup.StudentId" asp-items="Model.listStudents" class="form-control"></select>
                <span asp-validation-for="StudentGroup.StudentId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="StudentGroup.GroupId"></label>
                <select asp-for="StudentGroup.GroupId" asp-items="Model.listGroups" class="form-control"></select>
                <span asp-validation-for="StudentGroup.GroupId" class="text-danger"></span>
            </div>
            
                <button type="submit" class="btn btn-primary">Assign</button>
            <br/>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}